<h1><%= __("Dépannage") %></h1>

<p><%= __("Pour toute demandes générales, techniques ou besoin d'aide, vous avez plusieurs choix :") %></p>

<ul>
  <li><%= __("Le forum :") %> <a href="https://forum.recalbox.com/">https://forum.recalbox.com/</a></li>
  <li><%= __("Le chan IRC :") %> <a href="https://kiwiirc.com/client/irc.freenode.net/#recalbox">https://kiwiirc.com/client/irc.freenode.net/#recalbox</a></li>
  <li><%= __("Le wiki :") %> <a href="https://github.com/recalbox/recalbox-os/wiki/Home-(FR)">https://github.com/recalbox/recalbox-os/wiki/Home-(FR)</a></li>
</ul>

<br />

<form method="post">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title"><%= __("Effectuer une capture d'écran") %></h3>
    </div>
    <div class="panel-body">
      <p><%= __("Le bouton ci-dessous vous permet de prendre une capture d'écran de l'affichage actuel de recalbox.") %></p>
      <p><%- __("Le résultat sera sauvegardé dans le dossier %s.", "<code>" + raspi2png.savePath + "</code>") %></p>

      <p>
        <button type="submit" name="screenshot" value="1" class="btn btn-success"><%= __("Faire une capture") %></button>
      </p>

    <% if (typeof screenshotPath !== 'undefined') { %>
      <p>
        <%= __("Capture d'écran enregistrée :") %> <code><%= screenshotPath %></code></a>
      </p>
    <% } %>
    </div>
  </div>

  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title"><%= __("Exécution de recalbox-support.sh") %></h3>
    </div>
    <div class="panel-body">
      <p><%- __("Si on vous demande d'envoyer le résultat du script %s, vous pouvez le faire automatiquement ci-dessous.", "<code>recalbox-support.sh</code>") %></p>
      <p><%= __("Vous obtiendrez alors un lien de téléchargement qu'il vous suffira de copier / coller sur le forum.") %></p>

      <p>
        <button type="submit" name="support" value="1" class="btn btn-success"><%= __("Exécuter le script") %></button>
      </p>

    <% if (typeof downloadUrl !== 'undefined') { %>
      <p>
        <%= __("Lien de téléchargement à donner :") %> <a href="<%= downloadUrl %>" target="_blank"><%= downloadUrl %></a>
      </p>
    <% } %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6">
      <div class="panel panel-warning">
        <div class="panel-heading">
          <h3 class="panel-title"><%= __("Redémarrage et arrêt d'EmulationStation") %></h3>
        </div>
        <div class="panel-body">
          <button type="submit" name="es" value="restart" class="btn btn-warning"><%= __("Redémarer ES") %></button>

          <button type="submit" name="es" value="stop" class="btn btn-danger"><%= __("Arrêter ES") %></button>

          <button type="submit" name="es" value="start" class="btn btn-success"><%= __("Démarrer ES") %></button>
        </div>
      </div>
    </div>
    <div class="col-md-6">
      <div class="panel panel-danger">
        <div class="panel-heading">
          <h3 class="panel-title"><%= __("Redémarrage et arrêt du système") %></h3>
        </div>
        <div class="panel-body">
          <button type="submit" name="system" value="reboot" class="btn btn-warning"><%= __("Redémarer le système") %></button>

          <button type="submit" name="system" value="shutdown" class="btn btn-danger"><%= __("Arrêter le système") %></button>
        </div>
      </div>
    </div>
  </div>
</form>
